<Window x:Class="RFSTASIS_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RFSTASIS_Launcher"
        mc:Ignorable="d"
        Title="MainWindow"
        SizeToContent="WidthAndHeight"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
        TextElement.FontFamily="Sans"
        MinWidth="700"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Window.DataContext>
        <local:VM/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Margin="5 5 5 5">
                    <TextBlock FontSize="25" Text="RF STASIS"/>
                    <TextBlock FontSize="10" Text="CLASSIC RFONLINE REBORN"/>
                </StackPanel>
            </DockPanel>
            <DockPanel>
                <materialDesign:Card Padding="32" Margin="16" DockPanel.Dock="Right">
                    <StackPanel>
                        <TextBlock Text="LOGIN DETAILS"/>
                        <TextBox/>
                        <TextBox/>
                        <WrapPanel Margin="0 10 0 0">
                            <StackPanel Margin="0 0 5 0">
                                <WrapPanel>
                                    <TextBlock VerticalAlignment="Center" Text="WINDOW MODE"/>
                                    <CheckBox Margin="5 5 5 5"/>
                                </WrapPanel>
                                <Button Margin="0 5 0 0" Content="LOGIN"/>
                            </StackPanel>
                            <StackPanel Margin="5 0 0 0">
                                <WrapPanel>
                                    <TextBlock VerticalAlignment="Center" Text="MUSIC"/>
                                    <CheckBox Margin="5 5 5 5"/>
                                </WrapPanel>
                                <Button Margin="0 5 0 0" Content="SETTINGS"/>
                            </StackPanel>
                        </WrapPanel>
                    </StackPanel>
                </materialDesign:Card>
                <TextBlock/>
            </DockPanel>
            <materialDesign:Card Padding="32" Margin="16">
                <TextBlock Text="{Binding ServerStatus,UpdateSourceTrigger=PropertyChanged}"/>
            </materialDesign:Card>
            <Button Content="Start" Command="{Binding Start}"/>
        </StackPanel>
    </Grid>
</Window>
